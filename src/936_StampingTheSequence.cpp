#include "utils2.hpp"

class Solution {
  public:
    vector<int> movesToStamp(string stamp, string target) {
      vector<int> res;

      int m = stamp.size();
      int n = target.size();

      int i = 0;
      while (i + m < n) {
        int j = i;
      }

      return {};
    }
};


int main() {
  UFUNC(Solution::movesToStamp);
  return 0;
}